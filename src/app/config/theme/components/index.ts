import {ActionsheetContent, ActionsheetHeader} from './actionsheet';
import {Button} from './button';
import {FlatList} from './flatlist';
import {Flex} from './flex';
import {
  FormControl,
  FormControlErrorMessage,
  FormControlHelperText,
  FormControlLabel,
} from './formControl';
import {Heading} from './heading';
import {Input} from './input';
import {
  Modal,
  ModalBody,
  ModalCloseButton,
  ModalContent,
  ModalHeader,
} from './modal';
import {Pressable} from './pressable';
import {Select, SelectItem} from './select';
import {ScrollView} from './scrollView';
import {Spacer} from './spacer';
import {Text} from './text';

const components = {
  ActionsheetContent,
  ActionsheetHeader,
  Button,
  // FlatList,
  // Flex,
  FormControl,
  FormControlErrorMessage,
  FormControlHelperText,
  FormControlLabel,
  Heading,
  // Input,
  // Modal,
  ModalBody,
  ModalCloseButton,
  ModalContent,
  ModalHeader,
  // Pressable,
  Select,
  SelectItem,
  // ScrollView,
  // Spacer,
  Text,
};

export default components;
