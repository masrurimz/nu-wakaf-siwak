import {themeTools} from 'native-base';
import {colors} from '../colors';

export const Pressable = {
  // baseStyle: (props: any) => ({
  //   _pressed: {
  //     // bg: themeTools.mode('primary.50', 'primary.900')(props),
  //     opacity: 0.7,
  //   },
  // }),
  // defaultProps: {
  //   // android_ripple: {
  //   //   color: themeTools.mode(colors.primary[200], colors.primary[700])({}),
  //   // },
  // },
};
